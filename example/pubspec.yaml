name: outliner_view_example
description: Example app demonstrating the outliner_view library
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Depend on the parent library
  outliner_view:
    path: ../

  # State management (required for example app)
  flutter_riverpod: ^2.6.1

  # Material icons for the example app
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
